\input{../../../headers/tpheaders.tex}

\ifdef{\public}{\cleardoublepage}{}

\prob{Déterminer une loi d'entrée/sortie géométrique} 

\titlemod{Modéliser la loi d'entrée/sortie}

\begin{center}
\def\svgwidth{0.5\linewidth}
   \input{img/Maxpid_cinematique.pdf_tex}
\end{center}


Des données sur le système sont disponibles ici: \urlsysteme.

\question{Écrire les vecteurs $\overrightarrow{AB}$, $\overrightarrow{AC}$ et $\overrightarrow{BC}$ dans les bases respectives $B_0(\overrightarrow{x_0},\overrightarrow{y_0},\overrightarrow{z_0})$, $B_3(\overrightarrow{x_3},\overrightarrow{y_3},\overrightarrow{z_3})$ et $B_1(\overrightarrow{x_1},\overrightarrow{y_1},\overrightarrow{z_1})$. On mesurera $\|\overrightarrow{AO}\|$, $\|\overrightarrow{OB}\|$ et $\|\overrightarrow{BC}\|$ directement sur le système et on prendra $\|\overrightarrow{AC}\|=l(t)$ variable.}

\question{Donner la relation qui existe entre ces trois vecteurs.}

\question{Projeter cette relation dans la base $B_0(\overrightarrow{x_0},\overrightarrow{y_0},\overrightarrow{z_0})$ afin d'obtenir deux équations scalaires. On fera apparaître les angles $\theta_1$ et $\theta_3$.}

\question{A l'aide de ces deux relations faire disparaître $l(t)$ afin de trouver une relation entre $\theta_1$ et $\theta_3$.}

\question{Mettre cette relation sous la forme $\theta_1=f(\theta_3)$.}

\question{Mettre cette relation sous la forme $\theta_3=f(\theta_1)$.}

\newpage

\titleexp{Vérifier la relation $\theta_3=f(\theta_1)$.}

Télécharger le fichier \solidworks.

\question{Ouvrir le fichier assemblage du Maxpid et vérifier sont paramétrage. On pourra vérifier en déplaçant les pièces à la main que les contraintes ont été correctement mises en place.}

\medskip

Télécharger le fichier \matlabsimscape.

\question{Simuler le modèle simulink (version 2016a), vérifier les données affichées.}

\question{Recopier la formule de la première partie dans le bloc fonction et comparer les résultats des deux modèles.}

\newpage

\proceduresimscape

\end{document}

\newpage

\pagestyle{correction}

\section{Correction}



\end{document}
